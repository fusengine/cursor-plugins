# APEX task: Cursor global-default and project installation

## Subject

Correct the Cursor plugin collection so its primary installer is user-global under `~/.cursor/`,
while preserving explicit project scope, non-destructive ownership, transactional updates, and
focused public documentation.

## Owner corrections

- “tu regarde comment fonctionne le harness pour adapté”
- “on est d'accord il ne touche pas au harness si il veulent faire des correction il me donne le prompt”
- “on a notre harness corriger sur l'ordinateur tu peux regarder mais pas toucher”
- “les mécaniques son dans les hook”
- “~/.cursor/ c'est le chemin juste pour un repos global”
- “pourquoi ca n'install pas dans le repos .cursor et par un chemin relatif cursor le lit?”
- “pourquoi tous n'est pas sectionné dans un dossier doc??”
- “tu as l'exemple de la doc sur claude code a coté comment c'est structuré niveau doc et installer”
- “on a passé 2 jours dessus pourque je voient des bug ? c'est une blague”
- “bordel de dieu je t'ai donner un exemple a porter sur cursor”
- “oui mais ca fais depuis 16 heure tu sais ca”
- “tu corrige mais ma question pourquoi c'est aussi long”
- “./install.sh --project /chemin/absolu/du/projet comment ca😑 c'est pas ici ~/.cursor/”

## Phase progression

- Analyze: Cursor project plugin loading, current installers, harness boundary, and adjacent
  documentation structure inspected.
- Plan: project-local copy, native `workspaceOpen` loader, non-destructive hook merge, project rule,
  targeted uninstall, thin entry points, sectioned docs.
- Execute: RED integration contract captured, implementation completed, GREEN contract passed.
- eLicit: first challenge rejected manifest-independent loading, delete-before-copy reinstall, and
  loose root technical docs. The second challenge rejected loader containment, dangling-symlink
  detection, pre-existing hook ownership, and post-commit backup cleanup semantics. Different
  manifest-first, transactional, containment, ownership-receipt, and best-effort cleanup hypotheses
  were implemented with RED/GREEN regressions. Responsibility parity with the adjacent Claude
  implementation is now documented row by row. A final adversarial self-check found and corrected
  managed-root, plugins-root, and installed-inventory symlink substitution before the v3 gate.
  The v3 challenge then rejected command-wide hook ownership, non-preflight uninstall, omitted
  adjacent installer responsibilities, and a Claude reference in the public README. Exact hook
  origin, transactional uninstall rollback, a complete 16-row parity matrix, and a Cursor-only
  public README were implemented with RED/GREEN regressions. The v4 challenge then rejected hook
  identity after an identical post-install append and six omitted setup-runner responsibilities.
  A stable command token plus persisted occurrence now identifies the owned hook, and the complete
  matrix explicitly classifies 22 responsibilities. The v5 eLicit challenge confirmed the result.
- Verify: the first final gate was superseded when the sniper found four functional defects. After
  their corrections, eLicit v5 and the Verify challenger independently confirmed the current result.
- eXamine: the first sniper rejected stale hook-index handling, crash-backup recovery, non-atomic
  finalization, and managed-rule source updates. RED/GREEN regressions now cover each correction;
  the narrow eLicit and Verify challenges were confirmed, then superseded by the owner's global
  default correction.
- Scope reset: a fake-HOME RED proved the wrappers still required `--project`. The new dispatcher,
  global installer, ownership hashes, same-filesystem snapshot transaction, crash-window recovery,
  optional PowerShell project parameter, and global-first docs now pass the global integration test
  while all five project suites remain green.
- Final gates: the global-default eLicit challenger returned CONFIRMED, the global Verify challenger
  returned CONFIRMED, and the final global sniper returned PASS with zero in-scope errors. The task
  is functionally verified with Windows PowerShell and live Cursor activation still runtime-unverified.

## Timing integrity

The previous task `started_at` value was later than the measured current time. No trustworthy task
start source exists, so APEX records it as unknown instead of substituting the repository creation
time or another fabricated timestamp.

## Safety boundary

No harness, real user-global Cursor state, adjacent repository, or Git state may be modified during
development or validation. Global integration tests must use a fake home directory.
