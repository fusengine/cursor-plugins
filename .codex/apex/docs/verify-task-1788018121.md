# Verification — task-1788018121

**Date**: 2026-08-29T23:07:48Z
**Original request**: Analyze Cursor documentation and the owner-designated corrected local harness, adapt the Cursor plugins, never edit the harness, and provide the final harness-improvement prompt.

## Verdict

**FUNCTIONALLY RESOLVED for the requested repository-static scope.**

The Cursor plugin repository passes all requested and applicable repository-static checks. The exact harness aggregate is unchanged between the recorded pre-work and final snapshots. This proves endpoint equality for the measured harness tree; it does not claim that the harness worktree was clean.

Cursor UI/runtime behavior, installed state, `fuse-seo` utility scripts, Windows PowerShell execution, clean harness source/dist/local-packed parity, publishing, release, deployment, and version pinning remain explicitly unverified and excluded from this verdict.

## Acceptance criteria

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Analyze official Cursor behavior and the corrected local harness | PASS | Cursor Hooks, Plugins, Third-party Hooks, Skills, and Subagents documentation were cross-checked against the local harness source/package artifacts; unverified runtime behavior remains labelled as such. |
| 2 | Adapt the Cursor plugin repository | PASS | `./verify.sh --repository-only` reports `39 PASS, 0 FAIL, 1 WARN`; the warning is the known non-hook `fuse-seo/templates/json-ld/faq.json` `_note`, explicitly outside hook-only R16. |
| 3 | Keep repository declarations internally consistent | PASS | 24 marketplace entries match 24 plugin manifests; 196 skill names match their folders; 9 rules are ordered `00`–`08`; 1,159 explicit SKILL reference links resolve; 217 active-agent skill declarations resolve. |
| 4 | Preserve valid data and frontmatter | PASS | All 82 repository JSON files parse; all 2,448 frontmatter-bearing Markdown/MDC files pass `Psych.parse`. |
| 5 | Preserve hook integrity without an unsafe `0.1.90` pin | PASS | 21 hook manifests contain 63 entries: 62 unpinned commands plus one direct Stop prompt, with 0 version pins; aggregate `21f47bba156ce5bebfff03c1b91df709ef042932`. |
| 6 | Do not drift the owner-designated harness | PASS | The exact 11,818-file aggregate is `f8b5307424cbb2d070ea3cf0937108c81513d6a4` in both the recorded pre-work and final snapshots. No clean-worktree claim is made. |
| 7 | Provide a complete standalone harness-improvement prompt | PASS | `docs/harness-cursor-fix-prompt.md`: 187 lines, SHA-1 `81ec1bcb3f42c66d22739f203ef88c3bb190c9c2`, all required remaining implementation topics, all four owner corrections verbatim, and the manifest-trigger versus harness-mechanics boundary. |
| 8 | Preserve the owner's no-harness-edit boundary | PASS | Product work stayed in `cursor-plugins`. Hook manifests remain trigger/configuration surfaces; the harness mechanics executed by the 62 commands remain read-only and are addressed only by the standalone prompt. |

## Fresh executable evidence

### Syntax, lint, and repository suite

```text
bash -n install.sh verify.sh
PASS

shellcheck install.sh verify.sh
PASS

./verify.sh --repository-only
39 PASS, 0 FAIL, 1 WARN
```

### `FUSE_SOLID_MAX_LINES` boundaries

The host sets `BASH_ENV` and `FUSE_SOLID_MAX_LINES`, so these probes neutralized `BASH_ENV` to test the verifier itself:

```text
BASH_ENV=/dev/null env -u FUSE_SOLID_MAX_LINES /bin/bash verify.sh --repository-only
PASS: resolved to default 200; 39 PASS, 0 FAIL, 1 WARN

BASH_ENV=/dev/null FUSE_SOLID_MAX_LINES=187 /bin/bash verify.sh --repository-only
PASS: resolved to 187; 39 PASS, 0 FAIL, 1 WARN

BASH_ENV=/dev/null FUSE_SOLID_MAX_LINES=0 /bin/bash verify.sh --repository-only
PASS: rejected with exit 1 and "must be a positive integer"
```

### Behavioral probes

- 7/7 embedded Bash templates pass `bash -n` independently.
- `Write` content `one\ntwo\n` reports a prospective line count of 2.
- A unique literal `Edit` succeeds without changing the target.
- A duplicate literal `Edit` exits 2 unless `replace_all=true`.
- The same duplicate `Edit` succeeds with `replace_all=true`.

### Synchronization and references

- `AGENTS.md` and `fuse-rules/user-rules/fuse-global.mdc` are byte-identical from `## Identity` onward: 87 lines.
- Explicit Markdown reference links declared by SKILL files: 1,159, missing: 0.
- Active-agent skill declarations: 217, unresolved: 0.

## Integrity evidence

### Hook manifests

```sh
find . -path '*/hooks/hooks.json' -type f -print0 | sort -z | xargs -0 shasum | shasum | awk '{print $1}'
# 21f47bba156ce5bebfff03c1b91df709ef042932
```

### Harness read-only aggregate

The pre-work and final snapshots used the same command from `cursor-plugins`:

```sh
find ../../claude-code/fuse-harness -type f -print0 | sort -z | xargs -0 shasum | shasum | awk '{print $1}'
# 11,818 regular files
# f8b5307424cbb2d070ea3cf0937108c81513d6a4
```

This broad aggregate includes regular files under `.git`, `dist`, `node_modules`, caches, and ignored paths. It establishes equality between the two measured endpoints, not clean harness build/package parity and not a clean Git worktree.

## Owner corrections preserved verbatim

- "tu regarde comment fonctionne le harness pour adapté"
- "on est d'accord il ne touche pas au harness si il veulent faire des correction il me donne le prompt"
- "on a notre harness corriger sur l'ordinateur tu peux regarder mais pas toucher"
- "les mécaniques sont dans les hooks"

## Regression and side-effect review

- Repository suite: no failures; one pre-existing/out-of-scope warning remains visible rather than hidden.
- No setup, installation, marketplace sync, user-configuration write, harness build, package operation, publish, release, version pin, commit, or Git mutation was performed.
- The full verifier was not run because it reads installed Cursor state; repository-only mode was used intentionally.
- All task writes remained inside `cursor-plugins`; no harness, installed Cursor state, package cache,
  Git state, or publication path was modified.

## Independent challenge

The final eLicit claim is **CONFIRMED** after adversarial retries corrected stale 8/66 counts,
competing fixed file ceilings, and permissive verifier contracts. A subsequent Verify challenge
found no product contradiction and returned `REFUTED` only because this persisted artifact still
contained the older 36-pass, 178-line, three-correction evidence. This refresh records the current
39-pass, 187-line, four-correction state. The final read-only Verify rechallenge inspected this
frozen artifact and returned **CONFIRMED** with no repository-static contradiction.

## Deferred manual/runtime validation

1. Confirm all 24 plugins appear in Cursor Customize after reload.
2. Confirm restricted subagent `tools:` are enforced by the Cursor runtime.
3. Confirm the 9 plugin rules appear in Cursor Settings → Rules.
4. Confirm `fuse-global` appears under User Rules and is marked Always.
5. Execute the `fuse-seo/package.json` utility scripts when their domain validation is requested.
6. Execute Windows PowerShell installer validation on Windows.
7. In the harness repository, implement and prove the standalone prompt with source + clean `dist` + local packed-tarball parity before any version bump, publishing, deployment, or pinning.
